<Window x:Class="ItoKonnyaku.Commons.Windows.SeacretResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ItoKonnyaku.Commons.Windows"
        mc:Ignorable="d"
        Title="シークレット" Height="300" Width="300" FontSize="14" Closing="Window_Closing">
    
    <DockPanel>
        <Grid DockPanel.Dock="Bottom">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="公開"   Click="USendResultBtn_Click"/>
            <Button Grid.Column="1" Content="削除"   Click="UDeleteResultBtn_Click"/>
            <Button Grid.Column="2" Content="閉じる" Click="UCloseWindowBtn_Click"/>

        </Grid>
        <ListBox Name="uResultList" ItemsSource="{Binding}"/>

    </DockPanel>
</Window>
